---
"@riacoding/workflow-world-aws": patch
---

**BREAKING**: renamed from `@workflow-worlds/aws` to `@riacoding/workflow-world-aws` for private publishing under the `@riacoding` npm scope. Update `WORKFLOW_TARGET_WORLD` and any `pnpm add`/import references to the new package name.
